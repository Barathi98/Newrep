html 
head 
 script. 
  function countpara()
  {
     var totalpara=document.getElementsByTagName("p");
     alert("number of paragraphs "+totalpara.length);
  }
p This is a paragraph
p here is a another p tag. Here I'm using get element by tag name
p so this is an example
button(onclick="countpara()")  count paragraph
